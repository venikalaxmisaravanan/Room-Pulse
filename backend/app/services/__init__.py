"""Application logic (intentionally empty in this stage).

The availability engine, the occupancy simulator and the sensor-freshness
rules will live here so that route handlers stay thin and easy to read.
"""